/* Loaded before existing modules. Both desktop and embedded dashboards use the same service routes. */
if (window.MR_LOCAL_HOST) window.MR_SERVICES = {ecom: '/api/services/ecom', coolpaths: '/api/services/coolpaths', sam: '/api/services/sam'};
